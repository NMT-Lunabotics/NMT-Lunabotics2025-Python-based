





ros2 pkg create --build-type ament_python simple_chat


ssh masterpi@129.138.174.18



GITHUB_USER=<your_github_username>
GITHUB_TOKEN=<your_personal_access_token>
BRANCH=benjaminstestbranch
REPO=https://github.com/NMT-Lunabotics/NMT-Lunabotics2025-Python-based.git
DEST=/home/masterpi/NMT-Lunabotics2025-Python-based
docker ps -aq | xargs -r docker rm -f
sudo rm -rf $DEST
mkdir -p $DEST
cd $DEST
git clone -b $BRANCH https://$GITHUB_USER:$GITHUB_TOKEN@github.com/NMT-Lunabotics/NMT-Lunabotics2025-Python-based.git .
git fetch origin
git reset --hard origin/$BRANCH
git clean -fd
cd ros/ros2_ws
chmod +x ./build.sh




git remote add origin git@github.com:NMT-Lunabotics/NMT-Lunabotics2025-Python-based.git
git checkout -b benjaminstestbranch
git fetch origin
git merge origin/benjaminstestbranch
git add -A
git commit -m "Initial commit of ROS workspace"
git push -u origin benjaminstestbranch
