mkdir -p src
cd src
ros2 pkg create --build-type ament_python simple_chat


ssh masterpi@129.138.174.18

docker ps -aq | xargs -r docker rm -f
sudo rm -rf /home/masterpi/NMT-Lunabotics2025-Python-based
mkdir -p /home/masterpi/NMT-Lunabotics2025-Python-based
cd /home/masterpi/NMT-Lunabotics2025-Python-based
git clone -b benjaminstestbranch git@github.com:NMT-Lunabotics/NMT-Lunabotics2025-Python-based.git .
git fetch origin
git checkout benjaminstestbranch
git reset --hard origin/benjaminstestbranch
git clean -fd
git pull origin benjaminstestbranch
cd ros/ros2_ws
chmod +x ./build.sh
cp /home/masterpi/.env /home/masterpi/NMT-Lunabotics2025-Python-based/ros/ros2_ws


touch .env
nano .env



git remote add origin git@github.com:NMT-Lunabotics/NMT-Lunabotics2025-Python-based.git
git checkout -b benjaminstestbranch
git fetch origin
git merge origin/benjaminstestbranch
git add -A
git commit -m "Initial commit of ROS workspace"
git push -u origin benjaminstestbranch


rm -rf folder_name

mkdir folder_name
